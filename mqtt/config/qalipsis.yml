campaign:
  name: mqtt-${random.shortuuid}
  configuration:
    validation:
      max-execution-duration: PT2M

logging:
  console: true
  root: INFO

events:
  root: WARN

report:
  export:
    console-live:
      enabled: false