campaign:
  name: jakarta-ee-messaging-${random.shortuuid}
  configuration:
    validation:
      max-execution-duration: PT30S

logging:
  console: true
  root: INFO

events:
  root: WARN

report:
  export:
    console-live:
      enabled: false
