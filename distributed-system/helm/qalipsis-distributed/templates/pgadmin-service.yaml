{ { - if .Values.pgadmin.enabled } }
apiVersion: v1
kind: Service
metadata:
  name: { { include "qalipsis.fullname" . } }-pgadmin
  labels:
    {{- include "qalipsis.labels" . | nindent 4 }}
    {{- include "qalipsis.selectorLabels" (dict "Release" .Release "component" "pgadmin") | nindent 4 }}
spec:
  type: { { .Values.pgadmin.service.type } }
  ports:
    - port: { { .Values.pgadmin.service.port } }
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    { { - include "qalipsis.selectorLabels" (dict "Release" .Release "component" "pgadmin") | nindent 4 } }
  { { - end } }
