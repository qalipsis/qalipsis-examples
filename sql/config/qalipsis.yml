campaign:
  name: sql-${random.shortuuid}
  configuration:
    validation:
      max-execution-duration: PT40S

report:
  export:
    console-live:
      enabled: false


events:
  root: info
  export:
    slf4j:
      enabled: false